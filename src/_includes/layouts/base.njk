<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} - Streats Dining</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/assets/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Roboto:wght@400;500&display=swap" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-800">

    {% include header or "partials/header.njk" %}

    <main>
        {{ content | safe }}
    </main>

    {% include "partials/footer.njk" %}
    
    <script src="/assets/script.js"></script>

        <div id="reservation-modal" class="modal-overlay hidden fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center p-4 z-50">
            <div class="modal-content bg-white rounded-lg shadow-xl p-8 max-w-sm w-full text-center">
            <h2 class="text-2xl font-bold font-display mb-4">{{ ui.reservation_modal.title[lang] or ui.reservation_modal.title.en }}</h2>
            <p class="text-gray-700 mb-2">{{ ui.reservation_modal.prompt[lang] or ui.reservation_modal.prompt.en }}</p>
            <p class="text-3xl font-bold text-streats-red mb-6">
                {{ ui.reservation_modal.phone[lang] or ui.reservation_modal.phone.en }}
            </p>
            <button id="modal-close-button" class="bg-gray-700 text-white px-6 py-2 rounded-full hover:bg-gray-800 transition duration-300">
                {{ ui.reservation_modal.close_button[lang] or ui.reservation_modal.close_button.en }}
            </button>
        </div>
    </div>
    </body>
</html>

</body>
</html>