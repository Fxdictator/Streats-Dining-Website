# =========================================================================
# Dependencies
# =========================================================================
# The node_modules folder contains all the project's dependencies.
# It can be very large and should never be committed to Git.
# It can be fully recreated by running `npm install`.
node_modules


# =========================================================================
# Build Output
# =========================================================================
# This is the folder where Eleventy generates the final website.
# It should be ignored because it's generated automatically during the build process.
# Your hosting provider (like Netlify) will create this folder on their server.
public


# =========================================================================
# Log and Debug Files
# =========================================================================
# These files are often generated during development and contain debugging information.
npm-debug.log*
yarn-debug.log*
yarn-error.log*


# =========================================================================
# Environment Variables
# =========================================================================
# The .env file is used to store secret keys and environment-specific variables.
# It should never be committed to Git for security reasons.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local


# =========================================================================
# Operating System Files
# =========================================================================
# These are system-specific files that don't belong in a shared code repository.
.DS_Store
Thumbs.db